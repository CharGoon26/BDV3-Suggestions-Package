[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "suggestions-pkg"
version = "1.0.0"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["suggestions_pkg"]
